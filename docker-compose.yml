version: '3'

services:
  ingester:
    restart: always
    image: amalthundiyil/cm-ingester:latest
    build:
      context: './src/ingester'
    container_name: cm-ingester
    command: sh -c "uvicorn app.main:app --proxy-headers --host 0.0.0.0 --port 80"
    environment:
      MAPS_API_KEY: ${MAPS_API_KEY}
    ports:
      - "8000:80"
    volumes:
      - .:/usr/app/src
  user:
    restart: always
    image: amalthundiyil/cm-user:latest
    build:
      context: './src/user'
    container_name: cm-user
    command: sh -c "uvicorn app.main:app --proxy-headers --host 0.0.0.0 --port 80"
    environment:
      SECRET_KEY: ${SECRET_KEY}
    ports:
      - "8001:80"
    volumes:
      - .:/usr/app/src
  gateway:
    restart: always
    image: amalthundiyil/cm-gateway:latest
    build:
      context: './src/gateway'
    container_name: cm-gateway
    command: sh -c "uvicorn app.main:app --proxy-headers --host"
    environment:
      TOKEN_SECRET_KEY: ${TOKEN_SECRET_KEY}
    ports:
      - "8001:80"
    volumes:
      - .:/usr/app/src
volumes:
  data:
